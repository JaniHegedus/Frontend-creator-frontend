name: Qodana Code Quality Check

on: [push, pull_request]

jobs:
  qodana:
    runs-on: ubuntu-latest

    steps:
    - name: Check out code
      uses: actions/checkout@v2

    - name: Run Qodana
      uses: JetBrains/qodana-action@v2023.3
      with:
        token: ${{ secrets.QODANA_TOKEN }}
        # Other Qodana-specific parameters
